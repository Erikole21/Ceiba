# AGENTS.md

Propuesta de arquitectura edge-to-cloud para el Sistema Anti-Hurto de Vehículos — evaluación técnica Ceiba (Coach Técnico). Autor: Erik Rodríguez.

## Documentación del proyecto

- [summary.md](.agents/summary.md) — Alcance del repo, tabla de stack, reglas Always/Never/Ask. Leer al inicio de cualquier sesión.
- [testing.md](.agents/testing.md) — **Leer antes de ejecutar o acotar tests en workflows SDD** (`/refacil:apply`, `/refacil:test`, `/refacil:verify`, `/refacil:bug`). Contiene la política de ejecución por defecto (scoped) y los comandos específicos del repo.
- [architecture.md](.agents/architecture.md) — Seis pilares, componentes principales y flujos del sistema propuesto. Leer cuando se trabaja con diagramas o decisiones de diseño.
- [stack.md](.agents/stack.md) — Tabla de tecnologías propuestas, integraciones externas y configuración. Leer al evaluar opciones tecnológicas.
- [commands.md](.agents/commands.md) — Comandos disponibles (edición, lint, preview). Leer antes de ejecutar cualquier acción en el repo.

Paquete NPM de la metodología: [refacil-sdd-ai](https://www.npmjs.com/package/refacil-sdd-ai)

## SDD-AI Methodology

| Command | Description |
|---------|-------------|
| `/refacil:setup` | Install and configure the methodology in the repo |
| `/refacil:guide` | View flow guide and choose next step |
| `/refacil:explore` | Explore the codebase without modifying files |
| `/refacil:propose` | Create a change proposal (proposal, specs, design, tasks) |
| `/refacil:apply` | Implement the tasks of an approved proposal |
| `/refacil:test` | Run and review tests for the current change |
| `/refacil:verify` | Verify implementation against the specs |
| `/refacil:review` | Quality audit of the implemented code |
| `/refacil:archive` | Archive the change and sync specs to the historical record |
| `/refacil:up-code` | Update existing code to current patterns |
| `/refacil:bug` | Create a fix proposal for a detected bug |
| `/refacil:update` | Migrate documentation to the current methodology pattern |
| `/refacil:join` | Join the cross-repo bus (first service setup) |
| `/refacil:say` | Send a message to another agent via bus |
| `/refacil:ask` | Make a query to another agent and wait for response |
| `/refacil:reply` | Reply to a query received via bus |
| `/refacil:attend` | Attend and respond to pending bus messages |
| `/refacil:inbox` | View pending messages on the bus |

<!-- refacil-sdd-ai:compact-guidance:start -->
<!-- AUTO-GENERATED by refacil-sdd-ai. Do not edit between markers; content is refreshed every session. -->

## Token efficiency

Rules to minimize context consumption when working in this repository.

**File reading**
- Use `Read` with `offset` and `limit` when you know the relevant area of the file.
- Do not read files longer than 500 lines in full; segment by range.
- To discover repo structure: use `Glob` instead of `ls -R` or `find`.
- Do not re-read the entire file if you already read the relevant block; continue from that context.
- Before reading a long file, locate symbols with search and then read only candidate ranges.

**Search**
- Use `Grep` with `head_limit` instead of raw dumps.
- When you only need locations, use `output_mode: "files_with_matches"`.
- Always narrow with `glob` or `type` to avoid over-scanning.

**Git (via Bash)**
- History: `git log --oneline -20` by default; expand only if insufficient.
- Diffs: start with `git diff --stat`; request the full diff only for the file you are about to touch.
- Status: `git status -s` (short format).
- Show: `git show --stat` before the full show.

**Tests**
- Report only failures; do not enumerate passes in visible output.
- If the toolchain offers a quieter mode, use it (examples: **`pytest -q`**, **`jest --silent --reporters=summary`**, **`go test` with narrowed packages**, **`cargo test`** with `-p`/path filters — follow repo docs in `AGENTS.md` / `.agents/testing.md`).
- If the runner is verbose by default, truncate or summarize (e.g. last lines of stderr) instead of dumping everything.

**Logs and errors**
- Do not paste full stack traces; extract only the relevant frame with the message.
- Summarize repeated logs: "(N occurrences of <message>)".
- For log files: `tail -100` or filter by ERROR/WARN level.

**Build and lint**
- Type-check / lint (**stack-specific**: e.g. `tsc`, `eslint`, `ruff`, `mypy`, `golangci-lint`, `cargo clippy`, Maven/Gradle compile): group failures by file or rule; avoid line-by-line spam.
- Successful build: confirm with a single line, do not paste the full output.
- If there are many similar errors: show one example and the count.

**Docker and containers**
- `docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Image}}"`.
- `docker logs --tail 100` always; never full logs.

**General rule**
When in doubt between verbosity and conciseness, choose conciseness. The user can request detail on demand.
<!-- refacil-sdd-ai:compact-guidance:end -->
