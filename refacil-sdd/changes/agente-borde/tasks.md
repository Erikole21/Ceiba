# tasks: agente-borde

- [x] T-01: Redactar `docs/agente-borde/overview.md` — ciclo de vida del agente, diagrama Mermaid de flujo de datos interno entre los ocho subsistemas, restricciones de footprint (binario ~30 MB, < 5% CPU, < 50 MB RSS), glosario canónico del componente [M]
- [x] T-02: Redactar `docs/agente-borde/anpr-adapter-spi.md` — definir la interfaz SPI (contrato Go), especificar las cuatro implementaciones (REST local, socket UNIX, watch file, named pipe), esquema del dato de placa recibido, manejo de confidence, política de reconexión con backoff exponencial [M]
- [x] T-03: Redactar `docs/agente-borde/collector.md` — esquema completo del evento normalizado (campos obligatorios y opcionales), algoritmo de construcción del `event_id` (UUID v5: device_id + placa + timestamp_monotónico), reglas de timestamp dual (wall-clock UTC + monotónico), campo `clock_uncertain` [S]
- [x] T-04: Redactar `docs/agente-borde/bloom-filter.md` — parámetros del filtro (m bits, k funciones hash, FP ~1% para 1 M placas), formato de serialización binaria en disco, protocolo del topic MQTT retained para delta y sincronización completa inicial, tiempo máximo de sync (< 30 s en GSM 3G), modo degradado al arrancar sin filtro [M]
- [x] T-05: Redactar `docs/agente-borde/queue-manager.md` — esquema de la tabla SQLite (columnas: event_id, priority, payload_json, image_path, state, created_at, sent_at), configuración WAL (`synchronous=NORMAL`, `journal_mode=WAL`), política de prioridad FIFO con elevación HIGH para hits Bloom, secuencia de recuperación tras corte de energía, umbral de alerta de disco (500 MB) [M]
- [x] T-06: Redactar `docs/agente-borde/image-store.md` — estructura de directorios en filesystem, convención de nombres de archivo (device_id + timestamp + event_id), política de rotación (7 días o 10 GB), orden de eliminación (primero ya-subidas, luego más antiguas), no-blocking respecto a la captura [S]
- [x] T-07: Redactar `docs/agente-borde/uploader.md` — topics MQTT de publicación (`devices/<device_id>/events`), flujo completo de obtención de URL pre-firmada (request al Upload Service, TTL, PUT a S3-API), manejo de PUBACK y reintentos con backoff, actualización del campo `image_uri` en SQLite, idempotencia en reenvío (event_id como clave de deduplicación en cloud) [M]
- [x] T-08: Redactar `docs/agente-borde/health-beacon.md` — esquema JSON/msgpack del payload (CPU %, RSS MB, disco GB, batería %, RSSI GSM, drift NTP ms, eventos pendientes en cola, campos de alerta), topic de publicación (`devices/<device_id>/health`), intervalo por defecto (60 s), compresión (msgpack o CBOR), campos de alerta embebidos (`anpr_unavailable`, `disk_low`, `cert_expired`, `ota_error`, `clock_drift_warning`) [S]
- [x] T-09: Redactar `docs/agente-borde/config-ota-manager.md` — formato del manifiesto OTA (versión, URL, hash SHA-256, firma cosign), topics de suscripción (`devices/<device_id>/ota`, `fleet/<country_code>/ota`), procedimiento de descarga y verificación cosign, reemplazo atómico del binario (rename + systemd restart), rollback automático si el nuevo binario no arranca en N segundos, gestión de configuración remota (formato, versionado, aplicación sin reinicio); incluir especificación del parámetro `upload_mode` (`stolen_only` | `all`), su valor por defecto, el campo `authorized_by` requerido para activar `all`, y el log de auditoría del cambio de modo [M]
- [x] T-10: Redactar `docs/agente-borde/watchdog.md` — configuración systemd recomendada (`WatchdogSec`, `Restart=on-failure`, `RestartSec`, `StartLimitBurst`), criterios de emisión del keepalive desde el proceso Go (`sd_notify`), criterios de no-emisión (hang, deadlock, OOM), secuencia de recuperación de estado al reiniciar (carga SQLite, carga Bloom filter desde disco, resuscripción MQTT) [S]
- [x] T-11: Redactar `docs/agente-borde/security.md` — uso del certificado mTLS (rutas de sistema, referencia al change `pki-dispositivos` / ADR-010), restricciones de escritura en disco (solo Image Store y SQLite), verificación de firma OTA, modo "config locked" ante tampering detectado por watchdog, privacidad de datos (no se almacenan rostros ni biometría, solo placas y coordenadas, retención configurable por país, Ley 1581 y equivalentes) [S]
- [x] T-12: Redactar `docs/agente-borde/adr-local.md` — decisiones de diseño internas del agente que complementan ADR-001 (SQLite WAL: justificación de `synchronous=NORMAL` vs `FULL`), ADR-002 (keep-alive 60 s, sesión persistente, tamaño máximo de payload MQTT), ADR-009 (parámetros exactos del Bloom filter, justificación de delta vs descarga completa) [M]
- [x] T-13: Actualizar `docs/propuesta-arquitectura-hurto-vehiculos.md` — agregar referencias cruzadas en sección 2.3 y tabla 3.1 hacia `docs/agente-borde/`; no modificar diagramas Mermaid existentes ni atributos de calidad [S]
