# tasks: identidad-seguridad

- [x] T-01: Redactar `docs/identidad-seguridad/overview.md` — diagrama Mermaid C4 L2 con Keycloak, Vault, EMQX, API Gateway e IdPs externos (LDAP, BD, SOAP, SAML/OIDC) y sus relaciones; diagrama de flujo de autenticación humana (usuario → Web App → Keycloak → IdP país → JWT) y flujo de autenticación de dispositivo (bootstrap y renovación de certificado); tabla de componentes con responsabilidad; tabla de relaciones con changes relacionados (agente-borde, ingestion-mqtt, almacenamiento-lectura, api-frontend-analitica); decisiones ADR aplicadas (ADR-005, ADR-011) [M]

- [x] T-02: Redactar `docs/identidad-seguridad/adr-realm-por-pais.md` — evaluación de alternativas de modelo de realm (único realm con grupos, realm por país, clúster por país); decisión documentada (realm por país como barrera de aislamiento obligatoria por ADR-011); justificación de que grupos en un único realm no garantizan aislamiento técnico; consecuencias y límite de realms por clúster Keycloak [S]

- [x] T-03: Redactar `docs/identidad-seguridad/adr-vault-pki.md` — evaluación de alternativas de PKI (cfssl/step-ca, cloud-managed CA, Vault PKI Engine); criterios de decisión (cloud-agnostic, integración con secrets management, API automatizable, HA con Raft); decisión documentada (Vault PKI Engine detrás del puerto hexagonal `DevicePKIPort`); consecuencias y plan de operación [S]

- [x] T-04: Redactar `docs/identidad-seguridad/adr-spi-cache.md` — evaluación de estrategias de caché en SPI custom (sin caché, caché en memoria con TTL, caché persistente en Redis); criterios (disponibilidad ante IdP degradado, riesgo de credenciales comprometidas, complejidad); decisión documentada (caché en memoria TTL 5 min configurable); consecuencias y ventana de riesgo aceptada [S]

- [x] T-05: Redactar `docs/identidad-seguridad/jwt-claims-schema.md` — tabla canónica de claims obligatorios (`country_code`, `role`, `zone`, `sub`, `iss`, `exp`, `iat`, `jti`) con tipo, formato, restricciones y ejemplo; tabla de claims opcionales; ejemplos de payload JWT decodificado por cada uno de los cinco roles; regla de fijación del `country_code` desde la configuración del realm; algoritmo de firma RS256 con clave RSA ≥ 2048 bits; JWKS endpoint canónico; notas de contrato con almacenamiento-lectura, api-frontend-analitica y backbone-procesamiento [M]

- [x] T-06: Redactar `docs/identidad-seguridad/keycloak-realm-model.md` — diagrama Mermaid de la estructura realm → clientes → roles → proveedores de federación; tabla de clientes registrados por realm con tipo de flujo (authorization code + PKCE, client credentials, bearer-only); configuración de sesiones (access token TTL, refresh token TTL, rotación activa); configuración de SSO entre clientes del mismo realm; configuración de MFA por realm (TOTP, push, herencia del IdP país); política de contraseñas parametrizable; configuración del backend PostgreSQL de Keycloak (HA con Patroni on-prem o BD gestionada) [M]

- [x] T-07: Redactar `docs/identidad-seguridad/federation-ldap.md` — parámetros de configuración del User Storage SPI — LDAP (vendor, connection URL, bind DN, bind credential en Vault, user DN, object classes, atributos de ID); estrategia de sincronización on-demand vs. periódica con justificación; tabla canónica de mapeo LDAP → atributos Keycloak → claims JWT para `role`, `country_code`, `zone`, `email`, `given_name`, `family_name`; configuración SSL/TLS; política de solo lectura; comportamiento de Keycloak con caché de usuarios importados cuando el LDAP no está disponible [S]

- [x] T-08: Redactar `docs/identidad-seguridad/federation-jdbc.md` — parámetros de configuración del User Storage SPI — JDBC (JDBC URL, driver class, credenciales en Vault); queries configurables (autenticación, búsqueda por username, lista de usuarios); compatibilidad de drivers: PostgreSQL, MySQL, Oracle, SQL Server; tabla canónica de mapeo columna → atributo Keycloak → claim JWT (misma estructura que LDAP); consideraciones de pool de conexiones y timeout; nota sobre compatibilidad de algoritmos de hash legacy (MD5, SHA-1) y recomendación de migración [S]

- [x] T-09: Redactar `docs/identidad-seguridad/federation-spi-custom.md` — diagrama de secuencia Mermaid del flujo de autenticación (Keycloak → SPI custom → endpoint del país → caché → respuesta); contrato de interfaz del adaptador (Input/Output/Errores); especificación de la caché en memoria (estructura de clave, TTL configurable, sin persistencia); comportamiento en fallo upstream (sin bypass, backoff exponencial, métricas `spi.upstream.errors` y `spi.upstream.latency_ms`); empaquetado como JAR SPI de Keycloak o contenedor sidecar; registro como provider de Keycloak; guía de implementación de un adaptador nuevo para un país con endpoint SOAP [M]

- [x] T-10: Redactar `docs/identidad-seguridad/federation-saml-oidc.md` — diagrama de secuencia Mermaid del flujo SAML 2.0 Web SSO (SP-initiated) y del flujo OIDC Authorization Code con Keycloak como cliente; parámetros de configuración del Identity Provider en Keycloak para SAML y para OIDC (credenciales del IdP externo almacenadas en Vault); tabla de mapeo de aserciones/claims del IdP externo a atributos internos con regla de sobrescritura de `country_code`; configuración de "First Broker Login" para vincular identidades externas; pasos de onboarding para un nuevo país con IdP SAML [M]

- [x] T-11: Redactar `docs/identidad-seguridad/vault-pki-device.md` — diagrama Mermaid de la jerarquía de CAs (root CA → CAs intermedias `pki-{country_code}` → certificados de dispositivo); especificación completa del proceso de bootstrap (token de un solo uso, intercambio por primer certificado, invalidación del token); flujo de renovación automática con método `cert` auth (CSR 14 días antes de expiración, presentación del cert vigente, emisión del nuevo cert); flujo de revocación (`vault pki revoke`, publicación de CRL, verificación por EMQX); OCSP endpoint; configuración del rol de Vault PKI (`device-role`: allowed_domains, key_type RSA, key_bits 2048, ttl 90d); comandos Vault CLI de ejemplo para cada operación [L]

- [x] T-12: Redactar `docs/identidad-seguridad/vault-secrets-engine.md` — lista de secretos gestionados por Vault KV v2 por servicio (credenciales PostgreSQL de Keycloak, bind credentials LDAP, client secrets de clientes Keycloak, credenciales S3/MinIO, credenciales Kafka, API keys); Vault Database Secrets Engine con leases para credenciales de BD (24 h apps, 1 h migraciones); Vault Agent sidecar: configuración de templates para inyección como archivos en `/vault/secrets/` (nunca env vars); tabla de servicio → política de Vault → secretos accesibles; AppRole auth para servicios internos; Kubernetes auth para pods del clúster [M]

- [x] T-13: Redactar `docs/identidad-seguridad/vault-ha-unseal.md` — diagrama Mermaid de la topología HA de Vault (3 nodos Raft, 1 líder + 2 followers); configuración de Raft (`retry_join`, `performance_standby_nodes`); diagrama de flujo del auto-unseal hexagonal (`VaultUnsealPort` → implementaciones: AWS KMS, GCP KMS, Azure Key Vault, PKCS#11 HSM); configuración del adaptador (interfaz `Unseal(keyID string) ([]byte, error)`, selección por Helm value `unsealProvider`); runbook de inicialización (`vault operator init`, Shamir 5/3 para clave de emergencia); métricas de monitoreo (`vault.core.unsealed`, `vault.raft.leader`); política de backup del storage Raft [M]

- [x] T-14: Redactar `docs/identidad-seguridad/rbac-model.md` — tabla detallada de los cinco roles (`officer`, `supervisor`, `analyst`, `admin`, `auditor`) con permisos de lectura/escritura, scope de datos (`country_code` + `zone`), y endpoints autorizados en API Gateway; matriz de permisos por rol y recurso; reglas de herencia de roles (supervisor hereda de officer); regla de scope geográfico por `zone` para roles operacionales vs. scope de país completo para roles analíticos/administrativos; mapeo de roles a scopes OAuth 2.0; tabla de método HTTP + path pattern → roles permitidos para configuración del API Gateway; responsabilidad de enforcement (Keycloak emite, API Gateway verifica, servicios internos confían) [M]

- [x] T-15: Redactar `docs/identidad-seguridad/mtls-device-policy.md` — diagrama de secuencia Mermaid del handshake TLS 1.3 con certificado de cliente (agente de borde → EMQX: verificación de cadena de confianza, descarga de CRL, extracción de `device_id` del CN, evaluación de ACL); especificación completa de la ACL: tabla de patrón de tópico → operación → permitido/denegado; flujo alternativo para Upload Service (JWT de corta duración emitido por Vault TTL 5 min); configuración de EMQX: `listener.ssl.external.*` (cacertfile, verify, fail_if_no_peer_cert, crl_check, crl_cache_http_timeout) [M]

- [x] T-16: Redactar `docs/identidad-seguridad/multitenancy-security.md` — descripción del modelo de aislamiento (realm = barrera de autenticación, `country_code` = barrera de datos); diagrama Mermaid del flujo de propagación del `country_code` desde la autenticación hasta la capa de datos; listado de los cinco controles técnicos de aislamiento; escenario de prueba de penetración recomendado; política de acceso de administradores del sistema (superadmin Keycloak con MFA obligatorio y audit trail) [S]

- [x] T-17: Redactar `docs/identidad-seguridad/audit-authentication.md` — lista de tipos de eventos auditados por Keycloak (LOGIN, LOGIN_ERROR, LOGOUT, REFRESH_TOKEN, REFRESH_TOKEN_ERROR, CLIENT_LOGIN, TOKEN_EXCHANGE, REVOKE_GRANT, REGISTER y sus errores); schema del evento exportado (id, time, type, realmId, clientId, userId, sessionId, ipAddress, error, details JSONB); configuración del exportador de eventos (custom Event Listener SPI hacia OpenSearch o Loki); retención de 7 años; dashboard de Grafana/Kibana para tasa de intentos fallidos por país/usuario/IP; alerta: > 10 intentos fallidos del mismo usuario en 5 min → canal de seguridad [M]

- [x] T-18: Redactar `docs/identidad-seguridad/onboarding-nuevo-pais.md` — lista de pasos ordenados con responsable y herramienta: crear realm (Keycloak Admin API / Terraform), configurar clientes con client secrets en Vault, configurar proveedor de federación, verificar mapeo de atributos, crear CA intermedia en Vault, emitir certificado de CA firmado por root CA, registrar CA en EMQX, hacer bootstrap del primer dispositivo, verificar flujo end-to-end de autenticación de usuario, habilitar exportación de eventos de auditoría, actualizar documentación del país; tiempo estimado por tipo de IdP [S]

- [x] T-19: Redactar `docs/identidad-seguridad/helm/README.md` — values configurables para Keycloak (imagen, réplicas, PostgreSQL connection string desde Vault Agent, dominio, JVM heap, Ingress) y para Vault (imagen, nodos Raft, unsealProvider, KMS key ID, mounts de PKI por país); instrucciones de instalación, actualización (rolling update Keycloak, rolling restart Vault con re-unseal automático) y rollback; instrucciones de bootstrap post-instalación (init de Vault, montaje PKI raíz, emisión de CAs intermedias por país); diferencias de values para cloud vs. on-prem [S]

- [x] T-20: Redactar `docs/identidad-seguridad/terraform/README.md` — módulo Keycloak (realms, clientes, roles, proveedores de federación via `mrparkers/keycloak` provider); módulo Vault PKI (mount root, mounts intermedios por país, roles de emisión, políticas de revocación); módulo Vault Secrets (paths KV v2, políticas de acceso, AppRole per servicio, Kubernetes auth roles); variable `countries = ["CO", "MX", "AR"]` para provisioning en loop; salidas: JWKS endpoints y endpoints OCSP por país; orden de provisioning (Vault init → Vault Secrets → Keycloak PostgreSQL → Keycloak realms → Vault PKI → EMQX mTLS config) [M]

- [x] T-21: Actualizar `docs/propuesta-arquitectura-hurto-vehiculos.md` — agregar referencias cruzadas en la sección del Pilar 5 (Identidad federada) hacia `docs/identidad-seguridad/`; actualizar descripción del Pilar 5 con modelo de realm por país, PKI de Vault, cinco roles RBAC y política de mTLS; no alterar diagramas Mermaid existentes ni atributos de calidad objetivos [S]
