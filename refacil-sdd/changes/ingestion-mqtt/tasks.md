# tasks: ingestion-mqtt

- [x] T-01: Redactar `docs/ingestion-mqtt/overview.md` — descripción narrativa de la capa de ingestión, diagrama Mermaid de componentes (EMQX cluster, Upload Service, Object Storage, Bridge, Kafka), flujo completo device→cloud para metadatos y para imágenes, glosario canónico del pilar, ADRs aplicados (ADR-002, ADR-003, ADR-005, ADR-010, ADR-011) [M]

- [x] T-02: Redactar `docs/ingestion-mqtt/topic-schema.md` — tabla autoritativa de los seis tópicos MQTT (dirección, contenido, QoS, retained flag, tamaño máximo de payload), restricciones ACL por tópico, reglas de formato de `device_id` y `country_code`, ejemplos de payload JSON/msgpack para cada tópico [M]

- [x] T-03: Redactar `docs/ingestion-mqtt/adr-bridge-decision.md` — ADR formal con las dos opciones evaluadas (Kafka Connect + EMQX Data Bridge nativo vs. bridge Go custom), matriz de criterios (latencia, ordering, mantenimiento, ops, coste), decisión tomada (Kafka Connect + EMQX Rule Engine → Kafka Data Bridge), justificación y consecuencias [M]

- [x] T-04: Redactar `docs/ingestion-mqtt/emqx-cluster.md` — configuración de mTLS (cipher suites, verificación de cert, integración OCSP con Vault), ACL por `device_id` y `country_code` (reglas EMQX ACL o AuthZ plugin), `session_expiry_interval` recomendado (cálculo basado en número de dispositivos y presupuesto de memoria por nodo), keepalive de 60 s, límite de payload por tópico, parámetros de clúster HA (3 nodos, anti-affinity), configuración del Kafka Data Bridge en el Rule Engine [L]

- [x] T-05: Redactar `docs/ingestion-mqtt/upload-service.md` — especificación completa de la API REST (endpoints: `POST /v1/upload-url`, `POST /v1/upload` para proxy-upload, `GET /v1/health`), contrato de request/response con ejemplos, flujo de validación de identidad (verificación mTLS cert CN/SAN o JWT Bearer), lógica de generación de presigned URL (TTL 5 min, path `images/{country_code}/{device_id}/{date}/{image_id}`), modo proxy-upload (recepción de multipart, stream hacia object storage), puerto `ObjectStoragePort` y sus cuatro implementaciones adaptadoras, manejo de errores y códigos HTTP [L]

- [x] T-06: Redactar `docs/ingestion-mqtt/object-storage.md` — estructura de paths en el bucket (`images/`, `thumbnails/`, prefijos por `country_code` y fecha), política de lifecycle (imágenes completas: expiración 180 días; miniaturas: expiración 730 días), configuración de cifrado server-side (SSE-KMS, referencia a la clave KMS), política de bucket (deny sin cifrado, deny acceso público), configuración de replicación Multi-AZ, tabla de adaptadores S3-API (MinIO, AWS S3, GCS via interop, Azure Blob via interop) con notas de compatibilidad [M]

- [x] T-07: Redactar `docs/ingestion-mqtt/bridge.md` — especificación operativa del bridge según la decisión del ADR: configuración del Rule Engine de EMQX (reglas SQL para los tópicos de eventos, health e image-uri), configuración del Kafka Data Bridge (bootstrap servers, SASL/TLS, nombre del tópico `vehicle.events.raw`, clave de mensaje = `${clientid}`, serialización JSON, acks = all), manejo de backpressure (buffer interno EMQX, comportamiento ante Kafka no disponible), métricas expuestas (throughput, lag, errores), diagrama Mermaid del flujo de procesamiento de un evento [M]

- [x] T-08: Redactar `docs/ingestion-mqtt/security.md` — flujo mTLS completo (handshake, verificación CN/SAN, extracción de `device_id`), integración OCSP con Vault (endpoint, timeout, caché de respuesta, comportamiento ante OCSP no disponible), política de revocación CRL como fallback, rotación de certificados de dispositivo sin downtime (TTL 90 días, renovación anticipada a los 75 días), restricciones ACL detalladas con ejemplos de patrones de tópico, cifrado en tránsito (TLS 1.3 mínimo), cifrado en reposo (SSE-KMS), controles de privacidad (solo imágenes de vehículos, no biometría, retención máxima 24 meses, acceso por `country_code`) [M]

- [x] T-09: Redactar `docs/ingestion-mqtt/operations.md` — runbook de escalado horizontal de EMQX (métricas de alerta: conexiones por nodo, memory usage, message rate), procedimiento de gestión de tormenta de reconexiones (circuit breaker, backoff en el agente, rate limiting en el broker), rotación de CA Vault (procedimiento de doble CA durante la transición), monitoreo de lag del bridge Kafka (alerta cuando lag > 30 s), procedimientos de recuperación ante fallo de nodo EMQX, fallo del bridge y fallo del object storage [M]

- [x] T-10: Redactar `docs/ingestion-mqtt/helm/README.md` — guía de despliegue Kubernetes con Helm: dependencias de charts (EMQX Operator, MinIO Operator o Helm chart estándar), valores configurables clave por componente (réplicas, recursos, secretos, URLs de Vault/Kafka), ejemplos de `values.yaml` para entorno de desarrollo y producción, instrucciones de instalación (`helm install`), actualización rolling (`helm upgrade`) y rollback [M]

- [x] T-11: Redactar `docs/ingestion-mqtt/terraform/README.md` — guía de módulos Terraform: estructura de módulos (módulo `emqx-cluster`, módulo `upload-service`, módulo `object-storage`, módulo `kms-key`), variables de entrada y salida por módulo, ejemplos de uso para AWS, GCP y on-prem (MinIO), notas sobre el módulo `object-storage` como implementación del adaptador hexagonal (ADR-005) a nivel de infraestructura [S]

- [x] T-12: Actualizar `docs/propuesta-arquitectura-hurto-vehiculos.md` — agregar referencias cruzadas en la sección del Pilar 2 hacia `docs/ingestion-mqtt/`; actualizar la tabla de componentes de la capa de ingestión con enlaces a las especificaciones; no modificar diagramas Mermaid existentes ni atributos de calidad numéricos [S]
