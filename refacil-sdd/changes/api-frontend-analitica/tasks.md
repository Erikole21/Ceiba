# tasks: api-frontend-analitica

- [ ] T-01: Redactar `docs/api-frontend-analitica/overview.md` — diagrama Mermaid C4 L2 de la capa de API, frontend y analítica (API Gateway, seis microservicios, Web App, Superset); relaciones con `almacenamiento-lectura` (lecturas) y `backbone-procesamiento` (tópico `vehicle.alerts`); tabla de SLOs por servicio (search p95 < 300 ms / 800 ms, alertas p95 < 2 s end-to-end, analytics p95 < 3 s); perfiles de usuario y sus vistas; decisiones ADR aplicadas (ADR-005, ADR-008, ADR-011); glosario de componentes de esta capa [M]
- [ ] T-02: Redactar `docs/api-frontend-analitica/adr-api-gateway.md` — evaluación de Kong OSS, Envoy y NGINX Ingress; criterios (cloud-agnostic, validación JWT JWKS nativa, rate-limiting con Redis, extensibilidad hexagonal, complejidad operacional); decisión recomendada con justificación; consecuencias y adaptador hexagonal `ApiGatewayPort` para equivalentes gestionados en nube [S]
- [ ] T-03: Redactar `docs/api-frontend-analitica/adr-realtime-protocol.md` — evaluación de WebSocket (Socket.IO), Server-Sent Events y Long Polling; criterios (latencia p95 < 2 s, reconexión, enrutamiento por zona, complejidad); decisión recomendada (WebSocket/Socket.IO); consecuencias operacionales (sticky sessions o Redis adapter en K8s multi-réplica) [S]
- [ ] T-04: Redactar `docs/api-frontend-analitica/adr-cartography.md` — evaluación de MapLibre GL JS, Leaflet y OpenLayers; criterios (cloud-agnostic con tiles OSM, capas vectoriales para trayectorias, compatibilidad con Kepler.gl, licencia OSS); decisión (MapLibre GL JS + Kepler.gl); consecuencias: configuración de proveedor de tiles en Helm [S]
- [ ] T-05: Redactar `docs/api-frontend-analitica/api-gateway.md` — configuración de validación JWT (plugin JWKS, caché de claves con TTL 5 min, manejo de fallo del JWKS); extracción y propagación de claims `country_code`, `role`, `zone` como headers `X-Country-Code`, `X-Role`, `X-Zone`; rate-limiting (ventana deslizante, Redis `ratelimit:{tenant}:{endpoint}:{window}`, respuesta 429); enrutamiento por prefijo de ruta; TLS termination, HSTS, CORS; registro de auditoría; diagrama de secuencia Mermaid del flujo de autenticación y enrutamiento [M]
- [ ] T-06: Redactar `docs/api-frontend-analitica/search-service.md` — diagrama de secuencia del flujo de orquestación (API Gateway → OpenSearch → PostgreSQL+PostGIS → ObjectStoragePort); endpoint `GET /v1/plates/{plate}/events` con query params, esquema de respuesta JSON paginada (timestamp, lat/lon, device_id, confidence, thumbnail_url, country, city) y todos los códigos de error; puertos hexagonales `EventsIndexPort`, `EventsRepositoryPort` y `ObjectStoragePort`; estrategia de presigned URL GET con TTL 5 min; circuit breaker por puerto; SLO y métricas Prometheus; ejemplo completo de solicitud y respuesta [M]
- [ ] T-07: Redactar `docs/api-frontend-analitica/openapi/search-service.yaml` — especificación OpenAPI 3.1 con path `/v1/plates/{plate}/events`; esquemas `PlateEvent`, `PlateEventsResponse`, `ErrorResponse`; todos los parámetros, respuestas y ejemplos [S]
- [ ] T-08: Redactar `docs/api-frontend-analitica/alerts-service.md` — diagrama de secuencia del flujo Kafka → routing engine → WebSocket Gateway → cliente; schema del payload del tópico `vehicle.alerts`; endpoint REST `GET /v1/alerts?since=&country=`; gestión del estado de conexiones WebSocket en Redis (`{connection_id → country_code, zone, officer_id}`); manejo de desconexión y reconexión; Redis adapter de Socket.IO para multi-réplica; puertos `AlertsKafkaPort` y `AlertsStatePort`; métricas Prometheus; ejemplo de solicitud/respuesta REST [M]
- [ ] T-09: Redactar `docs/api-frontend-analitica/openapi/alerts-service.yaml` — especificación OpenAPI 3.1 con path `/v1/alerts`; esquemas `Alert`, `AlertsResponse`; documentación del protocolo WebSocket como extensión [S]
- [ ] T-10: Redactar `docs/api-frontend-analitica/incident-service.md` — diagrama de estados del incidente (created → dispatched → escalated → recovered → closed) con transiciones y eventos Kafka; endpoints `POST /v1/incidents/{plate}/recovery-action`, `GET /v1/incidents/{plate}`, `PATCH /v1/incidents/{id}/status` con esquemas y códigos de error; reglas de transición por rol y estado actual; lógica del `CountrySyncPort` (write-back y comportamiento cuando no está soportado); schema de entrada en `audit_log`; schema del mensaje al tópico `incident.events`; métricas Prometheus [M]
- [ ] T-11: Redactar `docs/api-frontend-analitica/openapi/incident-service.yaml` — especificación OpenAPI 3.1 con los tres paths; esquemas `RecoveryAction`, `Incident`, `AuditEntry`, `StatusUpdate`; respuestas 200, 201, 400, 401, 403, 404, 409 [S]
- [ ] T-12: Redactar `docs/api-frontend-analitica/analytics-service.md` — tres endpoints con esquemas completos de solicitud/respuesta (heatmap: `[{h3_index, event_count, avg_confidence}]`, trends: `[{date, theft_count}]`, routes: pendiente definición); query SQL de ejemplo por endpoint sobre vistas ClickHouse; puerto `AnalyticsRepositoryPort`; estrategia de detección de vistas desactualizadas (campo `data_freshness_warning`, cabecera `X-SLO-Degraded: true`); SLO y métricas Prometheus [M]
- [ ] T-13: Redactar `docs/api-frontend-analitica/openapi/analytics-service.yaml` — especificación OpenAPI 3.1 con los tres paths; esquemas `HeatmapCell`, `TrendPoint`, `RouteSegment`; cabecera `X-SLO-Degraded` [S]
- [ ] T-14: Redactar `docs/api-frontend-analitica/devices-service.md` — endpoints `GET /v1/devices`, `POST /v1/devices`, `GET /v1/devices/{id}`, `POST /v1/devices/{id}/ota`; campos del modelo de dispositivo; flujo OTA asíncrono (202 → `pending_ota`); aislamiento multi-tenant por `country_code`; métricas Prometheus [S]
- [ ] T-15: Redactar `docs/api-frontend-analitica/openapi/devices-service.yaml` — especificación OpenAPI 3.1 con los cuatro paths; esquemas `Device`, `OtaRequest`, `OtaResponse`; respuestas 200, 201, 202, 401, 403, 404 [S]
- [ ] T-16: Redactar `docs/api-frontend-analitica/vehicles-service.md` — endpoints `GET /v1/vehicles`, `POST /v1/vehicles`, `PATCH /v1/vehicles/{id}`; mapeo al schema canónico de `stolen_vehicles`; restricción de rol `admin`; incremento de `schema_version` en PATCH; inserción en `audit_log`; coordinación con la caché Redis `stolen:{country_code}:{plate_normalized}` al activar/desactivar un vehículo [S]
- [ ] T-17: Redactar `docs/api-frontend-analitica/openapi/vehicles-service.yaml` — especificación OpenAPI 3.1 con los tres paths; esquemas `StolenVehicle`, `VehiclePatch`; respuestas 200, 201, 400, 401, 403, 404 [S]
- [ ] T-18: Redactar `docs/api-frontend-analitica/webapp-architecture.md` — estructura de módulos (search, alerts, incidents, analytics, admin); gestión de estado con JWT y `country_code` como tenant scope global; integración con API Gateway (interceptor Axios/Fetch); módulo de mapa (MapLibre GL JS: capa de puntos, popup, centrado en alerta); módulo de alertas (Socket.IO, reconexión, polling al reconectar); módulo analítico (Kepler.gl + gráficos de tendencias); i18n es/en; embed de Superset; estructura de carpetas; diagrama de flujo de autenticación en la UI [L]
- [ ] T-19: Redactar `docs/api-frontend-analitica/superset-integration.md` — configuración de Superset en Kubernetes (imagen OSS, variables de entorno, SECRET_KEY); conexión ClickHouse en Superset (SQLAlchemy URI, pool); datasets sobre vistas ClickHouse; dashboards recomendados (mapa de calor, tendencias, rutas); estrategia de integración con la Web App (iframe embed o enlace con SSO); Row Level Security por `country_code`; guía de despliegue [M]
- [ ] T-20: Redactar `docs/api-frontend-analitica/slo-observability.md` — tabla de SLOs verificables por servicio; métricas Prometheus por servicio (latencias, tasas de error, `ws.active_connections`, `alerts.delivered`, `incidents.created`, `gateway.auth.rejected`, `gateway.jwks_cache_miss`); dashboards Grafana recomendados; alertas operacionales (latencia search > SLO durante 5 min, lag Kafka > 30 s, tasa de error > 5 % en 1 min); configuración de OpenTelemetry y propagación de `X-Trace-ID` [M]
- [ ] T-21: Redactar `docs/api-frontend-analitica/helm/README.md` — subchart por microservicio y para Superset; values configurables (URLs de dependencias, credenciales como Secrets, réplicas, tiles MapLibre); diferencias cloud vs. on-prem; instrucciones de instalación, actualización y rollback; Ingress para TLS; recursos CPU/memoria recomendados [S]
- [ ] T-22: Redactar `docs/api-frontend-analitica/terraform/README.md` — módulo api-gateway (rutas, plugins, certificados TLS), módulo superset (BD de metadatos, conexión ClickHouse), módulo webapp (assets estáticos si aplica); variables de entrada/salida; orden de provisioning [S]
- [ ] T-23: Actualizar `docs/propuesta-arquitectura-hurto-vehiculos.md` — agregar referencias cruzadas en la sección del Pilar 6 hacia `docs/api-frontend-analitica/`; actualizar la tabla de componentes de API y frontend con enlaces a las especificaciones detalladas; actualizar la descripción de los tres perfiles de usuario con referencia al frontend; no alterar diagramas Mermaid ni atributos de calidad objetivos [S]
- [ ] T-24: Agregar nota de coordinación en `docs/almacenamiento-lectura/overview.md` — referenciar `docs/api-frontend-analitica/` como la capa que implementa los contratos de lectura; indicar los puertos hexagonales de este change que implementan esos contratos [S]
